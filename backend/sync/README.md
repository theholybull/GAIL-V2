# Sync Scaffold

This folder reserves the Phase 1 local-first sync layer:

- local SQLite as the primary working database
- cloud Postgres as the sync target
- PowerSync-compatible client sync concepts
- explicit private-memory exclusion rules

No live sync implementation is present yet.
